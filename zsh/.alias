#!/bin/zsh
alias tnl='npm run test && npm run lint'
alias connect-znc='ssh root@51.15.55.164'

# git
alias glgg='git log --topo-order --all --graph --pretty=format:"${_git_log_oneline_format}"'
alias glgb='git log --topo-order --pretty=format:"${_git_log_brief_format}"'

alias scw='scw --region=ams1'

# source secret aliases
if [ -e ~/.secret_alias ]; then
  source ~/.secret_alias
fi
